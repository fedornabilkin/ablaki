# Классический крафт

Расширение существующих `craft_*`; владелец — `user.id`, единственный кредитный счёт — `persone.credit`. Миграция `m260920_190000_extend_classic_craft` сохраняет старые строки и инвентари. Идентификаторы переносимого каталога — стабильные строковые `code`; числовые ID остаются внутренними.

Если исходные таблицы крафта ещё не создавались, эта же миграция создаёт только недостающие таблицы перед расширением. Семь исходных миграций распознают уже установленную новую схему и не пытаются пересоздать таблицы при последующем штатном migrate/up. Обе последовательности проверены на MySQL/PostgreSQL.

## API

Все запросы требуют существующий Bearer-токен аккаунта. Права на чужие инвентари клиент не задаёт.

| Метод | Путь | Ответ |
|---|---|---|
| GET | `/v1/craft` | items, recipes, categories (включая description), stations, skills, inventory (суммы), inventory_slots (реальные стопки), credit, charge_credits, доступность наборов, слоты |
| POST | `/v1/craft/command` | message, replayed, актуальный state |
| GET | `/v1/craft/history?envelope=1&page=1` | items, _meta; только операции владельца, по 20 записей |

Пример тела команды: `{"action":"craft","id":123,"quantity":2,"request_key":"unique-command-key-123"}`. `id` — рецепт для craft, предмет для use/discard/merge/transfer. Для starter/gather/buy_slots ID не требуется. Количество партий — целое 1–100; use/discard/transfer принимают 1–10000 единиц. Ключ 16–80 символов: латиница, цифры, `_`, `-`. Неизвестный результат запроса повторяется с прежним ключом; новый ключ означает новую операцию. Один ключ с другим действием, количеством, целевым слотом или ценой отклоняется.

`inventory_slots` содержит занятые стопки `{id, item_id, quantity, position, active}` рюкзака. `inventory` суммирует доступные активные ресурсы; содержимое сундуков и заполненные сундуки не расходуются рецептом. Команды use/discard принимают необязательный `slot_id` — ID строки. Операция затрагивает только указанный слот владельца с указанным предметом и не расходует другие стопки при нехватке. Старые команды без slot_id сохраняют прежние ключи повторов.

`transfer` принимает `slot_id`, `container_id` (0 — рюкзак, иначе ID сундука), `position` (с 1), `id` предмета и `quantity`. `merge` сохраняет `slot_id` и `target_slot_id`. `buy_slots` принимает `quantity` и подтверждённый `unit_price`; сервер проверяет собственную настройку цены. История оплаты использует `type=craft_slots`.

Состояние возвращает `permanent_slots`, `active_slots`, `slots_expire_at`, `server_time`, `inventory_settings`, а также `containers: [{id, capacity, durability, max_durability, slots}]`. ID сундука совпадает с ID его предмета в рюкзаке; вложенность запрещена. Назначение предмета хранится в `storage_kind`: none/chest/elixir. Эликсир пространства добавлен отдельным рецептом; настой мастерства сохранил прежний эффект.

Набор starter выдаётся один раз (тройной объём сырья), gather — раз в календарные сутки Europe/Moscow. Выдача определяется только `gather_quantity` активных материалов каталога. Нельзя запросить произвольный предмет. Лимит новых успешных команд — 30 в минуту на аккаунт. Повторы ранее принятых команд возвращаются без повторного изменения данных.

Все записи одной операции находятся в транзакции. Порядок блокировок: craft_meta → persone владельца. Сейчас короткая общая блокировка каталога также сериализует команды разных игроков; при росте нагрузки можно заменить её версионированием каталога и отдельными блокировками аккаунтов, сохранив совместимость с импортом. Баланс меняет общий CreditLedger, история записывается атомарно.

Ингредиенты расходуются; инструменты и предмет станции сохраняются. Если один предмет одновременно ингредиент и инструмент, требуется расходуемое количество плюс одна сохраняемая единица. В рюкзаке 50 ячеек, изначально 20 активных. Постоянный слот стоит 10 Cr; эликсир открывает 5 слотов на 7 дней. Сундук вмещает 10 стопок, начинает с прочности 100 и расходует 1 за помещение предметов. Все эти значения настраиваются администратором.

Предметы складываются по stack_size, сундуки — по одному. Сначала дополняются активные стопки, затем используются свободные ячейки. Недостаток места откатывает операцию. Истечение срока не уничтожает предметы: они остаются в закрытых ячейках и автоматически занимают освободившиеся активные. GET проецирует ту же раскладку без записи; следующая команда сохраняет позиции. Старое переполнение сверх 50 доступно под сеткой 10×5. Изношенный сундук позволяет забирать содержимое; удалить заполненный сундук нельзя.

Навык отдельный для каждой категории: уровень `1 + floor(experience / 100)`, максимум 100. Зависимый рецепт открывается после изготовления каждого requires-рецепта и достижения min_level. Расходники с use_xp дают опыт своей категории; остальные предметы не имеют скрытых эффектов. Экипировка пока является изделием инвентаря.

## Администратор

По решению владельца от 21.09.2026 списание кредитов **выключено по умолчанию**. Миграция `m260921_100000_craft_credit_switch` добавляет переключатель в `craft_meta`; до применения миграции новый код также не списывает кредиты. Рецепты сохраняют настроенные цены, но при `charge_credits=false` эффективная стоимость равна нулю, баланс не проверяется и не меняется, запись списания в history_balance не создаётся. Ингредиенты расходуются как обычно, craft_event сохраняет credit_change=0. Переключатель «Списывать кредиты за создание предметов» находится в каталоге админки и применяется через POST `/craft/catalog/settings` с CSRF и p-admin. Настройка блокируется в той же транзакционной последовательности, что и крафт, сохраняются автор и время изменения. Параметры команды игрока не могут включить оплату; повтор завершённой команды не пересчитывает её цену после переключения режима.

`/craft/catalog` в backend-приложении (тест: порт 3195). Нужны вход и `p-admin`; публичные приложения не могут исполнять эти контроллеры. Изменения требуют POST и CSRF. Старые служебные действия выдачи и крафта отключены.

В каталоге доступны редакторы категорий, предметов, станций и рецептов. Связи ингредиентов, инструментов и предшествующих рецептов задаются стабильными кодами. Сначала показывается результат проверки, затем администратор подтверждает сохранение. Нельзя изменить каталог между проверкой и применением без повторного просмотра.

Экспорт — JSON версии 1 с группами categories/items/stations/recipes. Импорт — полный каталог или пакет upsert с четырьмя группами (ненужные группы — пустые массивы), до 2 МБ. Отсутствующая в файле запись не удаляется. Проверяются типы, диапазоны, ссылки и циклы. Применение всего пакета атомарно. Имеющиеся игровые остатки, навыки и история не удаляются. Для отключения предмета/рецепта используется active=0; принадлежащие игрокам предметы остаются видимыми.

Начальный каталог скачивается в админке: 44 предмета, 34 рецепта, четыре станции. `php yii craft-setup/seed` импортирует его только при отсутствии classic-plank; повторный запуск сохраняет изменения администратора. При совпадении названия со старым каталогом добавляется суффикс «(крафт)», без замены старой записи.

## Развёртывание

После git pull / make up workflow применяет три миграции крафта: `m260920_190000_extend_classic_craft`, `m260921_100000_craft_credit_switch`, `m260924_160000_craft_storage`. Затем устанавливается начальный каталог либо добавляется отсутствующий эликсир без перезаписи редакторских правок. Test запускает `craft-setup/install-test` в `/var/code/ablaki` с `CRAFT_TEST_INSTALL=confirmed-test-checkout`. Production использует `craft-setup/install-production` и `CRAFT_PRODUCTION_INSTALL=confirmed-production-checkout`, только из master. Повтор использует штатный журнал миграций и сохраняет инвентари, каталог и настройки. Миграции необратимы через down.

Перед платным выпуском на MySQL требуется InnoDB для счёта, журнала и таблиц операций. В переданной production-схеме `history_balance` использует MyISAM: новая проверка отклоняет оплату до списания, пока журнал не станет транзакционным. Эта миграция не меняет движок финансового журнала. На тестовом PostgreSQL ограничение не требуется. Локальные SQLite-проверки не подтверждают блокировки MySQL/PostgreSQL; конкурентные покупки и переносы выполняются в изолированном CI.

Перед production проверить резервную копию и миграцию на копии соответствующей БД; затем применить эту миграцию и начальный каталог. Сам production требует отдельного поручения владельца. SQLite-тест не заменяет CI на MySQL/PostgreSQL с параллельными запросами.

Таймеры и очереди крафта, биржа предметов и её комиссии отложены владельцем. Платежи, мир/бои, экипировка с эффектами, износ и ремонт — отдельные этапы; текущая реализация их не имитирует.

## Результат тестового выпуска 20.09.2026

Backend test `9061049` (CI 35528554041), frontend test `a5b14a3` (CI 35528598658): успешно. Установлены схема и каталог. Исходный незакоммиченный план на VPS сохранён в `/var/code/ablaki/.git/craft-plan-backups/mvp-ablaki-craft-20260920T180415-8168.md`.

Test10: получен стартовый набор, выполнены 12 операций крафта до сундука, создано 13 записей истории. Платная операция списала ровно 1 Cr (3 → 2); повтор того же ключа вернул replayed=true без повторного списания. Реальный ответ API прошёл парсер фронтенда; Vue отрисовал 34 узла карты. Гостевой доступ к каталогу админки запрещён. Визуальная проверка телефона и админки под реальным p-admin ещё требуется: доступных браузеров в computer-use нет.

## Обновление мастерской 21.09.2026

Backend test `401ac3e`, CI `35633552673`: проверки MySQL/PostgreSQL и деплой успешны; миграция настройки применена, API возвращает `charge_credits=false`. Frontend test `d5f619a`, CI `35634146097`: 163 теста, сборка и деплой успешны, SHA подтверждён через deploy-version.txt. Панели мастерской объединены и скрываются поверх карты; ремёсла работают как фильтры, карта перетаскивается и масштабируется колесом/жестом, список и инвентарь показывают остатки и предупреждения.

На Test10 выполнены слиток → гвозди → сундук. Номинальная цена сундука 1 Cr, фактический баланс остался 2 Cr, событие сохранило credit_change=0. Сундуков стало два; повтор той же команды вернул replayed=true без изменений баланса и инвентаря. Переключатель включения оплаты проверен изолированными POST/CSRF/p-admin тестами. Визуальная проверка остаётся открытой: браузеры в текущем сеансе недоступны. Production не обновлялся.

Дополнение: инвентарь 10×10 и новая раскладка опубликованы в backend test `1a37e7a` (CI `35640626835`) и frontend test `d363d69` (CI `35641181102`). 169 frontend-тестов, сборка и MySQL/PostgreSQL проверки успешны. Реальное удаление Test10 по slot_id=1 уменьшило только выбранную стопку бревна 45 → 44; остальные стопки и 2 Cr сохранились. Повтор вернул replayed=true без дополнительных изменений. Списание кредитов остаётся выключенным. Корзина требует подтверждения количества и сохраняет ID слота при повторе запроса; старые предметы сверх нового лимита не теряются.
