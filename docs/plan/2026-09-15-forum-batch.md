# Серверная часть задач форума 142–151

Источник: http://94.250.251.94:3181/forum/read/64. Цель: подготовить все серверные изменения к общей тестовой выкладке.

## Группа A: Контракты и экономика
- [x] A1. №144/148: безопасные полные профили авторов тем и первое активное сообщение независимо от пагинации.
- [x] A2. №146: дополнение незавершённых игр ботом для дуэли через существующий cron, с резервированием средств и защитой повторов.
- [x] A3. №149: проверка старого алгоритма пароля перед текущим с сохранением обычного процесса авторизации.
- [x] A4. №151: обязательный хэш при получении, более длинные новые коды, старые коды сохраняются; получение и отмена атомарны.

## Группа B: Проверка
- [x] B1. Проверить ошибки, повторные запросы, балансы и истории в изолированной БД.
- [x] B2. Выполнить release-тесты и CI; выложить подготовленную пачку backend, затем frontend на test.

Backend CI 35016003457 успешен, опубликован `862ff31abb5bd6670da492b3325ce4c811e86c70`. Проверки MySQL/PostgreSQL включают конкурирующее получение/отмену переводов и запуск бота. На test API проверены автор темы и стартовое сообщение, выполнен перевод 1 Cr Test01 → Test02 → Test01 (№20–21) с отказом без хэша. Итоговые балансы восстановлены. Создание перевода принимает только целые кредиты: фактическая схема `credit_transfer.amount` — integer.

## Группа C: Незавершённая проверка №149
- [ ] C1. Проверить вход реального старого аккаунта после исправления из [аудита авторизации](2026-09-18-authentication-audit.md). 18.09 владелец подтвердил: таблицы `users` нет, старые данные находятся в `user.salt` и `user.password_hash`. Предыдущее предположение об отдельном источнике было ошибочным.

№149 пока не отмечен кредитом как выполненная задача. Все остальные сообщения №140–148,150–151 отмечены по 1 Cr от Test01 (user_id=37), итого 11 Cr; подтверждённый остаток 789 Cr.

Критерий готовности: все серверные задачи проверены; production не изменён.

При первоначальной выкладке локально прошли восемь release-наборов и lint, но тесты авторизации опирались на неверное предположение о схеме. Исправление от 18.09 читает MD5 и соль из `user`. Владелец запретил миграцию хэша при входе и отменил production-выкладку. Итоговая версия только добавляет проверку старого пароля, сохраняет исходные хэш и соль, оставляет прежнюю работу `auth_key`, времени входа и аудита. Схема, инфраструктура и секреты не меняются.
