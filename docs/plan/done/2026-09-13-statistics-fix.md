# Исправление статистики и тестовой выкладки

## Цель и контекст
Восстановить тестовую выкладку после ошибки PHP 7.3 в StatController, закончить периодную статистику без нарушения старого API. Работа в fix/09-statistics от origin/master с последующим включением в test.

## Группа A: Backend
- [x] A1. Устранить синтаксис PHP 7.4 и сохранить скалярные поля ответа.
- [x] A2. Проверить сегодня/вчера по московскому времени, границы суток, пустые данные и смену формата кеша.
- [x] A3. Проверить периоды рейтинга, поиск, пагинацию, старый контракт и неверный период через Yii dispatch на временной SQLite.
- [x] A4. Добавить явный PHP lint статистики в CI.
- [x] A5. Выполнить все семь изолированных проверок на PHP 7.3.33 с mbstring, pdo_sqlite и intl; изменённые PHP-файлы прошли lint.

## Группа B: Тестовое окружение
- [x] B3. Обновить изолированные fixtures авторизации и списков для счётчика forum_credits_sent; проверить, что полученные подарки не входят в отправленные.
- [x] B1. Включить исправление в test и дождаться успешной тестовой выкладки: [GitHub Actions 34755106898](https://github.com/fedornabilkin/ablaki/actions/runs/34755106898), код 9ae392e.
- [x] B2. Проверить реальный API с локальным фронтом: HTTP 200, periods и совместимые totals, все пять периодов рейтинга, вторая страница, пустой поиск, старый list-контракт, HTTP 400 для неверного периода и CORS для localhost:5173.

## Критерии готовности
CI и тестовая выкладка успешны; /v1/stat возвращает прежние totals и periods; фронт показывает данные тестового API. Production в рамках этой задачи не публикуется.

На момент проверки: 21 участник, 2358 игр в орлянку, 7 игр в сапёр, 61 тема, 139 сообщений, 15 передач и 36 сделок. Суточные счётчики равны нулю. Это реальные ответы тестового API, а не fixtures.
