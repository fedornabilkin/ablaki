# Повторная проверка авторизации

Цель: проверить старые и новые пароли после сообщения владельца об отказе входа на production.

Контекст: повторная проверка алгоритма запрошена 2026-09-18. Владелец подтвердил: `users` не существует, production хранит соль и хэш в `user.salt` и `user.password_hash`.

Причина: прежняя реализация ошибочно опиралась на исторический импорт и отдельную таблицу `users`, а любой непустой `password_hash` исключала из старой проверки. Отказ воспроизведён тестом на подтверждённой владельцем структуре до исправления. Теперь сервис читает только выбранную запись `user`, распознаёт старый 32-символьный MD5 и проверяет его с её солью. Современный хэш никогда не проверяется старым алгоритмом.

## Группа A: Проверка исходного алгоритма
- [x] A1. Проследить старую регистрацию и вход от `Controller::setPost` до `Model_Users::encodePassword`.
- [x] A2. Проследить текущие регистрацию, поиск пользователя, валидацию, обновление хэша и выдачу токена.
- [x] A3. Воспроизвести пропущенную обработку старого пароля тестом, падающим до исправления.

Старый контроллер выполняет `addslashes(htmlspecialchars(strip_tags(trim($password))))` перед `md5($password . md5($salt))`. Ранее сервис совместимости воспроизводил только MD5; обработка восстановлена только для старой проверки. По последующему прямому указанию владельца миграция хэша при входе удалена: пароль только сверяется, MD5 остаётся MD5, bcrypt остаётся bcrypt. `salt` и `auth_key` также не меняются при входе. Обновляются только `last_login_at` и учёт активности.

## Группа B: Исправление и регрессия
- [x] B1. Восстановить обработку только при проверке legacy-пароля; явно закрепить старые флаги `ENT_COMPAT | ENT_HTML401` и UTF-8.
- [x] B2. Проверить девять независимых векторов: обычный пароль, крайние пробелы, амперсанд, двойная и одинарная кавычки, обратный слеш, теги, HTML-сущность и кириллица.
- [x] B3. Первоначально проверить переход MD5 → bcrypt; затем по указанию владельца полностью удалить обновление хэша и заменить тесты проверками отсутствия записей при повторных входах.
- [x] B4. Проверить реальные действия регистрации и входа нового пользователя по логину/email, сброс, блокировку и подтверждение email.
- [x] B5. Проверить вход без `users`/`remote_db`, отсутствие соли, повреждённый хэш, неизвестного пользователя и отсутствие секретов в ответе API. Новые пароли работают и на схеме без `salt`.
- [x] B6. Выполнить PHP 7.3 lint изменённых файлов и полный `tests/release/auth-response.php`.
- [x] B7. Запретить тестовым SQL-триггером обновление любых полей `user`, кроме `last_login_at`, и сравнить снимки таблицы до/после входа. Проверки охватывают пароль, вход по ключу, старую/новую схему и отсутствующий ключ.

При отсутствующем `auth_key` парольный вход теперь возвращает ошибку настройки доступа, не создаёт ключ и не выдаёт пустой токен. Вход по ссылке использует существующий ключ без ротации. Отдельные операции регистрации, смены пароля и выхода сохраняют свою ответственность за изменение учётных данных.

## Группа C: Выкладка и проверка
- [x] C1. Получить от владельца фактическое расположение старых хэша и соли; воспроизвести ошибку этой структуры локально.
- [x] C2. Первоначальный вариант выложен в test: `658763e`, CI 35355590145 успешен, Test01 (ID 37) вошёл через API. Это ещё вариант с обновлением MD5; версия без записей пока проверена только локально.
- [ ] C3. Production-выкладка отменена владельцем после push `50222c8` в master. Не продолжать выкладку и не отправлять master без нового прямого поручения. Исправление без записей подготовлено отдельно в `fix/09-authentication-readonly`; удаление рабочих веток отложено.
- [ ] C4. Подтвердить вход реального старого аккаунта; до этого №149 не отмечать подарком.

Критерии готовности: регрессионные проверки и выкладка проходят, владелец подтверждает вход проблемной учётной записи. Проверки не создают production-аккаунты и не меняют production-балансы.

Локальная проверка версии без записей: 133 утверждения auth-response, PHP 7.3 lint. Новых выкладок после отмены production не запускалось.
