# Placeholder test domain; replace server_name and certificate paths together.
server {
    listen 80;
    server_name api-test.ablakin.ru;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name api-test.ablakin.ru;
    ssl_certificate /etc/letsencrypt/live/api-test.ablakin.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api-test.ablakin.ru/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:19882;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
